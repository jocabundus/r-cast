declare sub draw_blimpy(m as Mob ptr)
declare sub draw_cavedweller(m as Mob ptr)
declare sub draw_dragonfly(m as Mob ptr)
declare sub draw_zombie(m as Mob ptr)
declare sub draw_map_tile_item(m as Mob ptr)

#include once "../draw.bas"
